<Window x:Class="lightclip.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lightclip" xmlns:Windows="clr-namespace:lightclip.Windows"
        mc:Ignorable="d"
        Title="Settings" Height="260" Width="475" Icon="/Properties/icon.ico" MinWidth="400" MinHeight="200" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TreeView Padding="1,4,1,1" Margin="0,-1,0,0" Background="Transparent" x:Name="SettingCategories">
        </TreeView>
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" Padding="10,4,10,0">
            <StackPanel x:Name="SettingsList">
                <StackPanel.Resources>
                    <Style TargetType="Grid">
                        <Setter Property="Height" Value="25"/>
                        <Setter Property="Margin" Value="0,0,0,4"/>
                        <Style.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="Margin" Value="0,3,0,0"/>
                            </Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Width" Value="125"/>
                            </Style>
                            <Style TargetType="TextBox">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalContentAlignment" Value="Center"/>
                                <Setter Property="Width" Value="125"/>
                                <Setter Property="TextWrapping" Value="NoWrap"/>
                            </Style>
                            <Style TargetType="CheckBox">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                            <Style TargetType="DockPanel">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                            </Style>
                            <Style TargetType="Button">
                                <Setter Property="Padding" Value="10,1,10,1"/>
                                <Setter Property="Margin" Value="0,0,4,0"/>
                            </Style>
                            <Style TargetType="Slider">
                                <Setter Property="Width" Value="72"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="SmallChange" Value="1"/>
                                <Setter Property="Margin" Value="3,0,0,0"/>
                            </Style>
                        </Style.Resources>
                    </Style>
                </StackPanel.Resources>
            </StackPanel>
        </ScrollViewer>
        <Windows:AppInfoPanel Grid.Column="1" Visibility="Hidden" x:Name="InfoPanel"/>
    </Grid>
</Window>
